# CLAUDE.md

Use [`./AGENTS.md`](./AGENTS.md) as the canonical quick-start for this repo.

Key constraints:

- Browser-only global API: `window.VirtualGridTable`
- No TypeScript support yet
- No package/module build yet
- Row virtualization only; columns are horizontally scrolled, not virtualized
